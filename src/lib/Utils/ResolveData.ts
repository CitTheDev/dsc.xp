// export async function resolveData(guildId: string) {
//     const data = await fetchSchema(options.guildId);
//     const tempStorage = this.client.tempStorage.get(options.guildId);
//     if (!data && !tempStorage) return res(null);
// }